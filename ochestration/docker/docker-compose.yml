version: '3.8'

services:
  thunderstorm_service:
    image: thunderstorm:latest
    ports:
      - "8000:8000"
    deploy:
      replicas: 1
      update_config:
        order: start-first
      restart_policy:
        condition: on-failure